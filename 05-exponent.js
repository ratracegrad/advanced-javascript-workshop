/*
exponent operator is similar to Math.pow()
*/

let mathTotal = Math.pow(3, 4);
console.log('math.power() total is', mathTotal);

let exponentTotal = 3 ** 4;
console.log('exponent total is', exponentTotal);

