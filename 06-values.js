/*
Object.values()
*/

const countries = {
    BR: 'Brazil',
    DE: 'Germany',
    RO: 'Romania',
    US: 'United States of America'
};
console.log('the values are', Object.values(countries));

/*
We already had Object.keys available to use
*/
console.log('the keys are', Object.keys(countries));

// Classwork 1
// what do you think the following console.log will output?
console.log(Object.values('es8'));